#
# ЗАДАНИЕ:
# А также вам необходимо в отдельном текстовом файле описать список docker-команд
# для управления этим проектом
#

# Команда для сборки и запуска проекта, состоящего из двух контейнеров: postgres и python.
# Команда запускается из папки, содержащей соответствующий docker-compose.yaml.

docker-compose up -d

# Команда для подключения к интерфейсу psql извне контейнера (из задания 1.3 не-ПРО).

docker exec -it pg-1-3-pro psql -U postgres -d testdb
